/*class Solution {
    public String largestNumber(int[] nums) {
        String[] arr=new String[nums.length];
        for(int i=0;i<nums.length;i++){
            arr[i]=nums[i]+"";
        }
        Arrays.sort(arr,(s1,s2)->(s1+s2).compareTo(s2+s1));
        StringBuilder sb=new StringBuilder("");
        for(int i=nums.length-1;i>=0;i--){
            sb.append(arr[i]);
        }
        return sb.toString();
    }
}*/
class Solution {
    public String largestNumber(int[] nums) {
        String[] arr = new String[nums.length];
        
        for (int i = 0; i < nums.length; i++) {
            arr[i] = String.valueOf(nums[i]);
        }
        
        // Sort using custom comparator for largest number
        Arrays.sort(arr, (a, b) -> (b + a).compareTo(a + b));
        
        // Handle all zeros case
        if (arr[0].equals("0")) {
            return "0";
        }
        
        return String.join("", arr);
    }
}
/*class Solution {
    public String largestNumber(int[] nums) {
        String[] arr = new String[nums.length];
        
        for (int i = 0; i < nums.length; i++) {
            arr[i] = Integer.toString(nums[i]);   // slightly faster than String.valueOf
        }
        
        // Custom comparator
        Arrays.sort(arr, (a, b) -> (b + a).compareTo(a + b));
        
        // All zeros check
        if ("0".equals(arr[0])) {
            return "0";
        }
        
        // Manual StringBuilder is often faster than String.join() in some judges
        StringBuilder sb = new StringBuilder(arr.length * 10);  // rough capacity
        for (String s : arr) {
            sb.append(s);
        }
        
        return sb.toString();
    }
}
class Solution {
    public String largestNumber(int[] nums) {
        String[] s = new String[nums.length];
        
        for (int i = 0; i < nums.length; i++) {
            s[i] = Integer.toString(nums[i]);
        }
        
        Arrays.sort(s, (a, b) -> (b + a).compareTo(a + b));
        
        if (s[0].charAt(0) == '0') return "0";
        
        StringBuilder sb = new StringBuilder(s.length << 3); // ~8 chars per number
        for (String str : s) {
            sb.append(str);
        }
        
        return sb.toString();
    }
}
class Solution {
    public String largestNumber(int[] nums) {
        // Convert to String array (fastest conversion)
        String[] arr = new String[nums.length];
        for (int i = 0; i < nums.length; i++) {
            arr[i] = Integer.toString(nums[i]);
        }
        
        // Fast custom comparator - most critical for speed
        Arrays.sort(arr, (a, b) -> {
            String ab = a + b;
            String ba = b + a;
            return ba.compareTo(ab);        // descending order
        });
        
        // Early exit if all zeros
        if (arr[0].charAt(0) == '0') {
            return "0";
        }
        
        // Build result with StringBuilder (faster than String.join in most cases)
        StringBuilder sb = new StringBuilder(nums.length * 10);  // Pre-allocate capacity
        for (String s : arr) {
            sb.append(s);
        }
        
        return sb.toString();
    }
}*/
