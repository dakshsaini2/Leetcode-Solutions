/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
/*class Solution {
    public List<String> binaryTreePaths(TreeNode root) {
        List<String> ans=new ArrayList<>();
        if(root==null){
            return ans;
        }else if(root.left==null && root.right==null){
             ans.add("" + root.val);
             return ans;
        }
        List<String> leftans= binaryTreePaths(root.left); 
        List<String> rightans= binaryTreePaths(root.right);
        for(String str : leftans){
            ans.add(root.val + "->" + str);


        }
         for(String str : rightans){
            ans.add(root.val + "->" + str);
        }
        return ans;
    }
}*/
//Below is the Best and Optimized Solution given by Grok
class Solution {
    public List<String> binaryTreePaths(TreeNode root) {
        List<String> result = new ArrayList<>();
        if (root == null) return result;
        
        dfs(root, new StringBuilder(), result);
        return result;
    }
    
    private void dfs(TreeNode node, StringBuilder path, List<String> result) {
        if (node == null) return;
        
        int len = path.length();                    // Save current length for backtracking
        
        // Append current node
        if (len > 0) path.append("->");
        path.append(node.val);
        
        // If leaf node, add copy of path to result
        if (node.left == null && node.right == null) {
            result.add(path.toString());
        } else {
            dfs(node.left, path, result);
            dfs(node.right, path, result);
        }
        
        // Backtrack - remove what we added
        path.setLength(len);
    }
}
